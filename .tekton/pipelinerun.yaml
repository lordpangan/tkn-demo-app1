---
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: tkn-demo-app1.git
  annotations:
    # The event we target as seen from the webhook payload
    # this can be an array too, i.e: [pull_request, push]
    pipelinesascode.tekton.dev/on-event: "[pull_request, push]"

    # The branch or tag we target (i.e., main, refs/tags/*)
    pipelinesascode.tekton.dev/on-target-branch: "[main]"

    # Shared pipeline template from cnpe/tekton — PAC fetches at runtime.
    # Task annotations are still required here: PAC resolves tasks at the
    # PipelineRun level, not from the referenced Pipeline.
    pipelinesascode.tekton.dev/pipeline: "https://raw.githubusercontent.com/lordpangan/cnpe/main/tekton/pipeline-java-ci.yaml"
    pipelinesascode.tekton.dev/task: "[git-clone, maven, kaniko]"
    pipelinesascode.tekton.dev/task-1: "https://raw.githubusercontent.com/lordpangan/cnpe/main/tekton/task-semgrep.yaml"

    # How many runs we want to keep.
    pipelinesascode.tekton.dev/max-keep-runs: "5"
spec:
  params:
    # The variable with brackets are special to Pipelines-as-Code
    # They will automatically be expanded with the events from GitHub.
    - name: repo_url
      value: "{{ repo_url }}"
    - name: revision
      value: "{{ revision }}"
    # Per-commit image tag so Chains signs each SHA individually.
    - name: image-reference
      value: "kind-registry:5000/tkn-demo-app1:{{ revision }}"
  pipelineRef:
    name: java-ci
  workspaces:
    - name: source
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
    # This workspace will inject secret to help the git-clone task to be able to
    # checkout the private repositories
    - name: basic-auth
      secret:
        secretName: "{{ git_auth_secret }}"
    - name: maven-settings
      emptyDir: {}
